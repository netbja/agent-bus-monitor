# Bus Redis password. If unset, the broker and the Go clients both default to
# AgentBus2025!. Set a real secret for anything beyond a localhost-only bus,
# then copy this file to .env (which is gitignored).
REDIS_PASSWORD=change-me
